// Name: feat-snippet
// Snippet: :feat
// Exclude: true
import "@johnlindquist/kit";
await setSelectedText('✨ feat():')
//await keyboard.type('✨ feat():')

await keyboard.type(Key.Left)
await keyboard.type(Key.Left)

