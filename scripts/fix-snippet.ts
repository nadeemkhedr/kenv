// Name: fix-snippet
// Snippet: *:fix
// Exclude: true
import "@johnlindquist/kit";
await setSelectedText(`🐛 fix(${await arg()}): `)
//await keyboard.type(Key.Left)
//await keyboard.type(Key.Left)
